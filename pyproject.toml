[project]
name = "hh-relay"
version = "0.1.0"
description = "FastAPI relay for the official hh.ru vacancy API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.116.1,<0.117",
    "httpx>=0.28.1,<0.29",
    "mcp>=1.13,<2",
    "pydantic>=2.11.7,<3",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1,<9",
    "pytest-asyncio>=1.1.0,<2",
    "ruff>=0.12.9,<0.13",
    "uvicorn>=0.35.0,<0.36",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hh_relay"]

[tool.pytest.ini_options]
addopts = "-q"
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py313"

[tool.ruff.lint]
select = ["ALL"]
ignore = [
    "COM812",
    "D",
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["EM101", "PLR2004", "S101"]

[tool.ruff.format]
quote-style = "double"
